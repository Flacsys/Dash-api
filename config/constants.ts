export const PAGINATION = {
    DEFAULT_PAGE: 1,
    DEFAULT_LIMIT: 20,
    MAX_LIMIT: 200
};

export const ROLES = {
    ADMIN: 'admin',
    SUPERADMIN: 'superadmin'
};

export const FILES = {
    PARTICIPANTS_EXPORT: 'participants.csv'
};

export const DEFAULTS = {
    JWT_EXPIRES_IN: '1h',
    PORT: '4000'
};
